#!/bin/sh
if [ -z "$DISPLAY" ]; then
	echo "No DISPLAY set. Exiting."
	exit
fi

cat > ~/.vimrc.solarized <<EOF
" This file will be overwritten by ~/bin/solarized-*
set bg=dark
EOF
xrdb -merge ~/.solarized.resources.dark

if [ "$1" != "-n" -a "$1" != "--no-term" ]; then
	urxvtcd
fi
